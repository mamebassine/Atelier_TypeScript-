// Déclaration des variables
let nom: string = 'MOUSSA';
let age: number = 30;

// Fonction pour afficher les valeurs
function afficherInformations(nom: string, age: number): void {
  console.log(`Nom: ${nom}`);
  console.log(`Âge: ${age}`);
}

// Appel de la fonction pour afficher les valeurs
afficherInformations(nom, age);
